// Scoped style
import classes from "./style.module.scss";

export default function DocsPage() {
  return (
    <main className={classes.docsPage}>
      <h2>Recently released!</h2>
    </main>
  );
}
